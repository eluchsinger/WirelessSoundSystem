apply plugin: 'java'

repositories {
    mavenCentral()
}

task executeServer(type: JavaExec){

    main = "controllers.Main"
    classpath = sourceSets.main.runtimeClasspath
}

dependencies {
    compile project (':shared')
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}